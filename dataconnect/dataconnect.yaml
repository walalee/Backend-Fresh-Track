specVersion: "v1"
serviceId: "backend-fresh-track"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "backend-fresh-track-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
